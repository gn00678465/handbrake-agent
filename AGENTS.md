# AGENTS.md

**Always read [CLAUDE.md](./CLAUDE.md)**

## 動工前必做（不可略過）

- 讀 `docs/handoffs/` 內最新一份檔案（檔名 `YYYY-MM-DD-<topic>.md`，按日期排序取最後一個）
- 跑 `git log -5 --oneline` 看近期 5 個 commit
- 若 `docs/handoffs/` 不存在則跳過該步
